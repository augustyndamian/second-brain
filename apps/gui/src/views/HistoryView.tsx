// DEPRECATED — Phase 6 (sidebar History removal). This file is no longer
// imported by App.tsx; the Today date picker (day:view IPC) covers the same
// functionality. Kept as an empty stub so typecheck passes without orphan deletion.
//
// Safe to delete in a future cleanup.
export function HistoryView() {
  return null;
}
